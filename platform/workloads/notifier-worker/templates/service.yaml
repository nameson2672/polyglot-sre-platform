apiVersion: v1
kind: Service
metadata:
  name: {{ include "notifier-worker.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "notifier-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "notifier-worker.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
